export type {
  Difficulty,
  GateName,
  InputName,
  InputStates,
  LevelDefinition,
  LevelsByDifficulty,
} from "../core/gameTypes";

export type Screen =
  "intro" | "modeSelection" | "levelSelection" | "game" | "results";
